package controller;
import view.MainView;

public class MainController {
    private MainView view;

    public MainController(MainView view) {
        this.view = view;
    }

    // Métodos para manipulação da view e interação com o modelo
}
